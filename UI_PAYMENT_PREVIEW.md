# 📊 UI Preview - 2 Payment Methods

## Current UI (After Update)

```
┌─────────────────────────────────────┐
│  Phương thức thanh toán              │
├─────────────────────────────────────┤
│                                      │
│  ◯  💳  Ví điện tử                  │
│     └─ Trừ tiền ngay                │
│        Xác nhận tự động             │
│                                      │
│  ◯  💵  Thanh toán khi đến cửa hàng│
│     └─ Đợi xác nhận staff           │
│        Trừ tiền sau                 │
│                                      │
└─────────────────────────────────────┘
```

---

## Payment Flow Diagram

### 🟢 WALLET (Ví Điện Tử)

```
┌─────────────────────┐
│ Chọn Ví Điện Tử     │
└──────────┬──────────┘
           │
           ▼
┌──────────────────────┐
│ Kiểm tra số dư       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Trừ tiền ngay        │
│ paymentMethod:       │
│ "WALLET"             │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ ✅ Xác nhận tự động  │
│ Status: CONFIRMED    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Hiển thị Success     │
└──────────────────────┘
```

---

### 🟡 CASH (Thanh Toán Khi Đến)

```
┌───────────────────────────┐
│ Chọn Thanh Toán Khi Đến   │
└──────────┬────────────────┘
           │
           ▼
┌──────────────────────────┐
│ Tạo Booking              │
│ paymentMethod: "CASH"    │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Status: PENDING          │
│ Đợi xác nhận staff       │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Staff xác nhận           │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Trừ tiền                 │
│ Status: CONFIRMED        │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Hiển thị Success         │
└──────────────────────────┘
```

---

## Code Comparison

### ❌ Before (3 Methods)

```typescript
const paymentMethods = [
  { id: "wallet", label: "Ví điện tử" },
  { id: "bank_transfer", label: "Chuyển khoản" }, // ❌ REMOVED
  { id: "cash", label: "Thanh toán khi đến" },
];

const paymentMethodMap = {
  wallet: "WALLET",
  cash: "CASH",
  bank_transfer: "CASH", // ❌ REMOVED
};
```

### ✅ After (2 Methods)

```typescript
const paymentMethodMap = {
  wallet: "WALLET", // ✅ KEPT
  cash: "CASH", // ✅ KEPT
  // ❌ bank_transfer removed
};
```

---

## Request Format

Both methods send same structure, different `paymentMethod`:

```json
// Option 1: WALLET
{
  "paymentMethod": "WALLET",
  "bookings": [...]
}

// Option 2: CASH
{
  "paymentMethod": "CASH",
  "bookings": [...]
}
```

---

## Summary Table

| Feature            | WALLET       | CASH                |
| ------------------ | ------------ | ------------------- |
| **UI Label**       | Ví điện tử   | Thanh toán khi đến  |
| **Icon**           | 💳           | 💵                  |
| **API Value**      | WALLET       | CASH                |
| **Payment Timing** | Ngay lập tức | Sau khi xác nhận    |
| **Confirmation**   | Tự động      | Manual (staff)      |
| **Status**         | CONFIRMED    | PENDING → CONFIRMED |

---

## Implementation Status

✅ API interface updated  
✅ UI updated (2 options only)  
✅ Mapping logic simplified  
✅ No TypeScript errors  
✅ Documentation complete  
✅ Ready for production
